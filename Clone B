--code số 30b: CLONE B - BAY THUẦN, KHÔNG TRỒI LÊN, KHÔNG TELE
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local player = Players.LocalPlayer

local FIX = Vector3.new(563.2, 3.9, -20.8)
local FIX_STAND = Vector3.new(FIX.X + 3, FIX.Y, FIX.Z)
local RAD = 50
local FLY_SPEED = 200
local HEAD_OFFSET = 8
local MAX_SPEED = 18
local STUCK_TIMEOUT = 10

local act = false
local stb = false
local noc = false
local Y = nil
local En = false
local isFlying = false
local characterAddedConn = nil

local function get()
    local c = player.Character
    if not c then return nil, nil, nil end
    return c, c:FindFirstChild("Humanoid"), c:FindFirstChild("HumanoidRootPart")
end

local function enableNoclip(c)
    noc = true
    if not c then return end
    for _, v in pairs(c:GetDescendants()) do
        if v:IsA("BasePart") then v.CanCollide = false end
    end
end

local function disableNoclip(c)
    noc = false
    if not c then return end
    for _, v in pairs(c:GetDescendants()) do
        if v:IsA("BasePart") then v.CanCollide = true end
    end
end

local function enableFreefall(h)
    h.PlatformStand = true
    workspace.Gravity = 0
end

local function disableFreefall(h, r)
    h.PlatformStand = false
    h.AutoRotate = true
    workspace.Gravity = 196.2
    if r then r.Velocity = Vector3.zero end
    h.WalkSpeed = MAX_SPEED
end

local function flyToFix()
    if not act or isFlying then return end
    isFlying = true
    stb = false
    En = true
    
    local c, h, r = get()
    if not r or not h then 
        isFlying = false
        return 
    end
    
    local tg = FIX_STAND
    Y = tg.Y - HEAD_OFFSET
    enableFreefall(h)
    enableNoclip(c)
    r.CFrame = CFrame.new(r.Position.X, Y, r.Position.Z)
    r.Velocity = Vector3.zero
    local lastPos = r.Position
    local stuckTime = 0
    
    -- BAY NGẦM ĐẾN TOẠ ĐỘ X,Z
    while act and En and r and r.Parent do
        local flatCur = Vector3.new(r.Position.X, 0, r.Position.Z)
        local flatTgt = Vector3.new(tg.X, 0, tg.Z)
        local dist = (flatCur - flatTgt).Magnitude
        if dist <= 5 then break end
        local dir = (flatTgt - flatCur).Unit
        r.Velocity = Vector3.new(dir.X * FLY_SPEED, 0, dir.Z * FLY_SPEED)
        if math.abs(r.Position.Y - Y) > 0.5 then
            r.CFrame = CFrame.new(r.Position.X, Y, r.Position.Z)
        end
        h.AutoRotate = false
        if dir.Magnitude > 0.01 then
            r.CFrame = CFrame.lookAt(r.Position, Vector3.new(r.Position.X + dir.X, r.Position.Y, r.Position.Z + dir.Z))
        end
        if (r.Position - lastPos).Magnitude < 0.5 then
            stuckTime = stuckTime + 0.1
            if stuckTime >= STUCK_TIMEOUT then
                break
            end
        else
            stuckTime = 0
        end
        lastPos = r.Position
        task.wait()
    end
    
    En = false
    r.Velocity = Vector3.zero
    
    -- BAY LÊN MẶT ĐẤT (KHÔNG TRỒI LÊN TỪ TỪ, BAY THẲNG)
    if r and r.Parent then
        local targetY = tg.Y
        r.Velocity = Vector3.new(0, 50, 0)
        task.wait(0.1)
        while act and r and r.Parent and r.Position.Y < targetY do
            task.wait()
        end
        r.Velocity = Vector3.zero
        r.CFrame = CFrame.new(r.Position.X, targetY, r.Position.Z)
        task.wait(0.1)
    end
    
    if r and r.Parent then
        disableFreefall(h, r)
    end
    disableNoclip(c)
    h.WalkSpeed = MAX_SPEED
    Y = nil
    stb = true
    isFlying = false
end

local function onCharacterAdded()
    if act then
        task.wait(1)
        flyToFix()
    end
end

if characterAddedConn then characterAddedConn:Disconnect() end
characterAddedConn = player.CharacterAdded:Connect(onCharacterAdded)

if player.Character and act then
    task.spawn(function()
        task.wait(0.5)
        flyToFix()
    end)
end

-- GUI
if CoreGui:FindFirstChild("ACTUAL_TG_GUI_B") then CoreGui["ACTUAL_TG_GUI_B"]:Destroy() end
local g = Instance.new("ScreenGui", CoreGui)
g.Name = "ACTUAL_TG_GUI_B"
g.ResetOnSpawn = false

local mf = Instance.new("Frame", g)
mf.Size = UDim2.new(0, 200, 0, 100)
mf.Position = UDim2.new(0.5, -100, 0.5, -50)
mf.BackgroundColor3 = Color3.fromRGB(24, 24, 30)
mf.Active = true
mf.Draggable = true
Instance.new("UICorner", mf).CornerRadius = UDim.new(0, 12)

local s = Instance.new("UIStroke", mf)
s.Color = Color3.fromRGB(250, 50, 100)
s.Thickness = 2

local head = Instance.new("Frame", mf)
head.Size = UDim2.new(1, 0, 0, 30)
head.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
Instance.new("UICorner", head).CornerRadius = UDim.new(0, 12)

local title = Instance.new("TextLabel", head)
title.Size = UDim2.new(0.6, 0, 1, 0)
title.Position = UDim2.new(0.05, 0, 0, 0)
title.Text = "CLONE B SYSTEM"
title.TextColor3 = Color3.new(1, 1, 1)
title.Font = Enum.Font.GothamBold
title.TextSize = 11
title.BackgroundTransparency = 1
title.TextXAlignment = Enum.TextXAlignment.Left

local cls = Instance.new("TextButton", head)
cls.Size = UDim2.new(0, 22, 0, 22)
cls.Position = UDim2.new(1, -28, 0, 4)
cls.Text = "✕"
cls.TextColor3 = Color3.new(1, 1, 1)
cls.BackgroundColor3 = Color3.fromRGB(220, 50, 50)
Instance.new("UICorner", cls).CornerRadius = UDim.new(0, 4)

local min = Instance.new("TextButton", head)
min.Size = UDim2.new(0, 22, 0, 22)
min.Position = UDim2.new(1, -54, 0, 4)
min.Text = "─"
min.TextColor3 = Color3.new(1, 1, 1)
min.BackgroundColor3 = Color3.fromRGB(200, 150, 30)
Instance.new("UICorner", min).CornerRadius = UDim.new(0, 4)

local body = Instance.new("Frame", mf)
body.Size = UDim2.new(1, 0, 1, -30)
body.Position = UDim2.new(0, 0, 0, 30)
body.BackgroundTransparency = 1

local status = Instance.new("TextLabel", body)
status.Size = UDim2.new(1, 0, 0, 20)
status.Position = UDim2.new(0, 0, 0, 4)
status.Text = "🔴 Đang tắt"
status.TextColor3 = Color3.new(1, 0.3, 0.3)
status.BackgroundTransparency = 1
status.Font = Enum.Font.Gotham
status.TextSize = 11

local btn = Instance.new("TextButton", body)
btn.Size = UDim2.new(0, 110, 0, 35)
btn.Position = UDim2.new(0, 10, 1, -50)
btn.BackgroundColor3 = Color3.fromRGB(0, 150, 80)
btn.Text = "▶ START B"
btn.TextColor3 = Color3.new(1, 1, 1)
btn.Font = Enum.Font.GothamBold
btn.TextSize = 12
Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)

local cpy = Instance.new("TextButton", body)
cpy.Size = UDim2.new(0, 60, 0, 35)
cpy.Position = UDim2.new(1, -70, 0, 20)
cpy.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
cpy.Text = "📋 COPY"
cpy.TextColor3 = Color3.new(1, 1, 1)
cpy.Font = Enum.Font.GothamBold
cpy.TextSize = 11
Instance.new("UICorner", cpy).CornerRadius = UDim.new(0, 5)

min.MouseButton1Click:Connect(function()
    body.Visible = not body.Visible
    mf.Size = body.Visible and UDim2.new(0, 200, 0, 100) or UDim2.new(0, 200, 0, 30)
end)

cls.MouseButton1Click:Connect(function()
    act, En = false, false
    isFlying = false
    g:Destroy()
end)

cpy.MouseButton1Click:Connect(function()
    pcall(function() setclipboard(tostring(FIX)) end)
end)

btn.MouseButton1Click:Connect(function()
    if not act then
        act = true
        btn.Text = "🛑 STOP B"
        btn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
        status.Text = "🟢 Đang bay đến FIX..."
        status.TextColor3 = Color3.new(0.3, 1, 0.3)
        task.spawn(function()
            flyToFix()
        end)
    else
        act, En = false, false
        isFlying = false
        btn.Text = "▶ START B"
        btn.BackgroundColor3 = Color3.fromRGB(0, 150, 80)
        status.Text = "🔴 Đang tắt"
        status.TextColor3 = Color3.new(1, 0.3, 0.3)
        local _, h, r = get()
        if h and r then h:MoveTo(r.Position) end
    end
end)

task.spawn(function()
    while g and g.Parent do
        if act and stb then
            status.Text = "🟢 Đã sẵn sàng tại FIX"
            status.TextColor3 = Color3.new(0.3, 1, 0.3)
        elseif act and isFlying then
            status.Text = "🟡 Đang bay..."
            status.TextColor3 = Color3.new(1, 1, 0)
        elseif act then
            status.Text = "🟠 Đang khởi tạo..."
            status.TextColor3 = Color3.new(1, 0.5, 0)
        end
        task.wait(0.5)
    end
end)
